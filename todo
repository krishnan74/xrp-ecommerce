Day 1 
//Create landing page with signin button or signup button (clerk auth)
Inside the dashboard , create a connect wallet for XRP
    When xrp is connected store the wallet details in variable in global scope
    Use the wallet addreses to send XRP 

Changes to be made

    Link supa base user table to clerk 
    Join usertable and product table to match addreess and name
    when message for new user is sent then the URL has to be changed 
    Star rating has to be implemented 
    Render messages on the screen in chronological order along with their time of sent 
    Enable editing the products uploaded by users
    Implement Cart functionality and add confirmation for buy 
    
DEBUGS  
    Profile page is not rendered at first but renders only after refresh